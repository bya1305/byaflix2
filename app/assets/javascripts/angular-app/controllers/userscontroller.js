function UserController(UserService) {
  var ctrl = this;

};

angular
  .module('app')
  .controller('UserController', UserController)
  

/*  ask how to change this .run into a function I can use to actually pull info from onto page */
